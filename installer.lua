local a="compute"local b="ivanoliverfabra"local c="main"local d="https://raw.githubusercontent.com/"..b.."/"..a.."/"..c;local function e()local f=http.get(d.."/files.txt")if not f then return nil,"Failed to get files.txt"end;local g=f.readAll()f.close()local h={}for i in g:gmatch("[^\r\n]+")do table.insert(h,i)end;return h end;local function j(k)local l=http.get(d..k)if not l then return false,"Failed to download file"end;local g=l.readAll()l.close()return g end;local function m()local h=e()if not h then return false,"Failed to get files"end;for n,k in ipairs(h)do local g,o=j(k)if not g then return false,o end;local k=fs.open(a.."/"..k,"w")if not k then return false,"Failed to open file for writing"end;k.write(g)k.close()end;return true end;print("Installing "..a.."...")print("This may take a while")local p,o=m()if not p then error(o)end;print("Installation successful")fs.delete(shell.getRunningProgram())